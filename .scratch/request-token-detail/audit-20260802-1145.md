# 审计报告：request-token-detail

- 审计时间：2026-08-02 11:45（审计由 issue 04 承接，报告文件名经 `date +%Y%m%d-%H%M` 校正）
- 审计范围：spec.md + 4 张 issue + 实现提交（`4b90c1d`、`8cdc9b1`、`b4a94e3`、`d3ee7d8`）+ README EN/ZH + docs/adr/
- 审计方式：只读；代码直读 + `cargo test --lib` 独立复跑 + git 提交核对

## 证据分级说明

- L1：代码/文件直读、`cargo test --lib` 独立复跑（115 passed）、git 提交与 issue 文件核对
- L2：issue 实施总结自述（vitest 47 passed、`napi build --release` / `vite build` / `tsc --noEmit` 构建结果、真实日志人工验证数据）——受 bash 权限限制无法独立复跑，标注为 L2
- L3：无

---

## 维度一：完成度（每张 issue 的 Status 与 checkbox 是否与实际一致）

### issue 01 — 后端聚合（Status: resolved, commit 4b90c1d）— **达成，全部 7 项 checkbox 属实**

| # | checkbox | 结论 | 证据 |
|---|----------|------|------|
| 1 | `/stats` 响应新增 `recentRequests`：窗口内倒序、截断 100、ts 缺失排最后、空窗口空数组 | 达成 | L1：`src-rust/stats.rs:131-132` 字段 + `serde(rename="recentRequests")`；`:477-479` 排序（`cmp_ts_desc`，`:285-292`，RFC3339 字符串倒序、`None` 排最后）后 `truncate(100)`；空窗口 → 空数组（`aggregate_recent_requests_window_excluding_everything_yields_empty` 测试通过） |
| 2 | 明细行字段齐全（ts/provider/model/ok/status/error + 5 个 token 字段 + cacheRate），无 usage 行 token 为 null | 达成 | L1：`RecentRequest` 结构 `stats.rs:82-101`，token 字段均为 `Option<u64>`；`usage_of`（`:231-244`）对失败/retry/无 token 字段返回 `None` → `:429-434` null + `"-"` |
| 3 | cacheRate 三态规则 | 达成 | L1：`cache_rate_of` `stats.rs:273-281`：input=0 → `-`；cached=0 → `0.0%`；否则 `{:.1}%`，公式 cached ÷ input |
| 4 | totalTokens = input + output（推理不重复累加） | 达成 | L1：`stats.rs:441` `detail.total_tokens = Some(u.prompt + u.completion)`，与 ADR-0003 一致 |
| 5 | `ConversationStats` 新增 `cacheRate`，对话级同口径 | 达成 | L1：`stats.rs:77-78` 字段；`:470` `conv.cache_rate = cache_rate_of(conv.input_tokens, conv.cached_tokens)` |
| 6 | 既有聚合行为无回归 | 达成 | L1：改动为追加式（既有字段/逻辑未改）；`cargo test --lib` 独立复跑 115 passed，含既有窗口/对话/断路器用例 |
| 7 | 新增用例覆盖 7 类场景 + `cargo test` 全绿 | 达成 | L1：9 个新测试全部存在并通过（`aggregate_recent_requests_carry_full_usage_fields`、`..._serialize_with_camel_case_keys`、`..._cache_rate_three_states`、`..._no_usage_rows_keep_null_token_fields`、`..._respect_window`、`..._sort_desc_and_truncate_100`、`..._window_excluding_everything_yields_empty`、`..._sort_ts_missing_last`、`aggregate_conversation_cache_rate_three_states`） |

实施总结中「cargo test 115 passed、新增 9 个用例」经独立复跑核实一致。

### issue 02 — WebUI 明细表格与对话卡片（Status: resolved, commit 8cdc9b1）— **达成，全部 7 项 checkbox 属实**

| # | checkbox | 结论 | 证据 |
|---|----------|------|------|
| 1 | 类型同步（RecentRequest、recentRequests、cacheRate 可选，字段缺失不崩） | 达成 | L1：`webui/src/types.ts:162-190`：`RecentRequest`、`UsageStats.recentRequests?`、`ConversationStats.cacheRate?`（可选字段，缺失即 undefined） |
| 2 | 明细表格 10 列，按响应顺序（已倒序） | 达成 | L1：`StatsPanel.tsx:216-229` 表头 Time/Provider/Model/Status/Input/Output/Cached/Reasoning/Rate/Total；直接 map `recentRequests` 顺序 |
| 3 | 无 usage 行 `-`；失败行 status+error（截断）；成功行 status | 达成 | L1：`formatRequestToken`（null → `-`）、`formatRequestStatus`（`:307-313`：ok → status；失败 → status + error，`:253` `truncate` + `title`） |
| 4 | token 列复用缩写格式化、缓存率显示后端字符串 | 达成 | L1：`formatRequestToken` 包装 `formatTokenCount`（K/M/B 缩写）；缓存率 `r.cacheRate ?? "-"` 直显 |
| 5 | 对话行补齐输入/输出/缓存率，超宽两行布局，保留 Cached/Reasoning/Total | 达成 | L1：`StatsPanel.tsx:191-207`：上行会话短 id + requests 数，下行六项（Input/Output/Cached/Reasoning/Rate/Total） |
| 6 | 明细为空数组不渲染、不报错 | 达成 | L1：`:212` `stats.recentRequests?.length ? ... : null`；测试 `does not render the request details card when recentRequests is empty or absent` |
| 7 | 组件测试覆盖 4 类场景，vitest 全绿 | 达成（vitest 结果 L2） | L1：`StatsPanel.test.tsx:272-433` 5 个用例（有 usage 行 / 无 usage 与失败行 / 对话行新增项 / 空数组 x2）；vitest 47 passed 为 issue 自述（L2） |

### issue 03 — 收尾验证与文档（Status: resolved, commit b4a94e3 + d3ee7d8）— **4/5 达成，1 项部分达成**

| # | checkbox | 结论 | 证据 |
|---|----------|------|------|
| 1 | 原生模块与 WebUI 均构建成功，无类型错误 | 达成（构建结果 L2） | L2：issue 自述 `napi build --release` / `vite build` / `tsc --noEmit` 通过；`cargo test --lib` 已独立复跑通过（编译目标全绿） |
| 2 | 真实 `requests.log` 人工验证（窗口过滤/倒序/截断/`-`/对话三项） | 达成（L2） | L2：issue 自述含具体数据（21 行倒序、旧行 null、cached=0 行 `0.0%`、窄窗口过滤 7 行、对话级 97.8%/76.7%）；后端路径与 `aggregate` 逻辑、测试覆盖一致（L1 交叉印证） |
| 3 | README（EN/ZH）补充说明 | 达成 | L1：`README.md:140`、`README_ZH.md:139` 新增「Request details / 请求明细」条目；`README.md:139`、`README_ZH.md:138` 更新对话行六项两行布局 |
| 4 | 本 issue 标记 resolved 并附实现摘要 | 达成 | L1：issue 文件 Status 行与实施总结存在；`d3ee7d8` 为状态标记提交 |
| 5 | 审计复核通过（完成后按既有惯例运行一次审计） | **部分达成** | 勾选为完成，但审计实际尚未执行——由 issue 04（`ready-for-agent`）承接，issue 03 实施总结亦写明「审计复核报告见 issue 04」。勾选超前于事实，但流程上有意为之，属预期在途项 |

注：issue 03 Status 行标注的 commit 为 `b4a94e3`（README 文档提交，本 issue 的交付提交），状态变更提交为 `d3ee7d8`，二者共同构成，标注准确。

### issue 04 — 审计（Status: ready-for-agent）— **未执行属预期**

- 6 项 checkbox 全部未勾选，Status 为 `ready-for-agent`，与实际状态一致（本审计即为其工作内容）
- 本审计报告产出后，issue 04 仍需按流程人工标记 resolved 并附结论摘要（审计员不修改任何文件）
- 注意：`04-audit-spec-issues-and-readme.md` 与 `spec.md` 目前为 untracked（未提交），与本 feature 已提交内容无冲突，04 尚未执行属预期

---

## 维度二：spec 遵守（交付内容是否被实现且被 issue 覆盖，无遗漏无超范围）

### spec 交付内容逐项核对 — **达成**

| spec 声明 | 实现位置 | issue 覆盖 | 结论 |
|-----------|----------|------------|------|
| `UsageStats` 扩展 `recentRequests`（窗口过滤、倒序、截断 100、空窗口空数组） | `stats.rs:131-132, 339-344, 477-479` | 01 #1 | 达成 |
| `RecentRequest` 响应形状与 spec JSON 示例逐字段一致（含 camelCase、`cacheRate` 为字符串、token 为 null 语义） | `stats.rs:82-101`；测试 `aggregate_recent_requests_serialize_with_camel_case_keys` | 01 #2 | 达成 |
| cacheRate 规则四分支（无 usage → `-`；input=0 → `-`；cached=0 → `0.0%`；其余 `{:.1}%`） | `cache_rate_of` `stats.rs:273-281` | 01 #3 | 达成 |
| 收集逻辑在 `aggregate` 内、与既有聚合共用同一次遍历 | `stats.rs:339-445`（单一 `for entry in entries` 循环内构造 detail） | 01 | 达成 |
| 排序 `ts` 倒序（RFC3339 字符串比较）、缺失排最后、`truncate(100)` | `cmp_ts_desc` + `stats.rs:477-479` | 01 #1 | 达成 |
| `ConversationStats` 扩展 `cacheRate`，聚合后同规则计算 | `stats.rs:77-78, 468-471` | 01 #5 | 达成 |
| `totalTokens` = input + output，无 usage 为 null | `stats.rs:441` | 01 #4 | 达成 |
| WebUI：`types.ts` 接口、表格 10 列、`-` 处理、时间格式化 | `types.ts:162-190`、`StatsPanel.tsx:212-269`、`format.ts:39-55` | 02 #1-4 | 达成 |
| 「By conversation」两行布局六项 | `StatsPanel.tsx:191-207` | 02 #5 | 达成 |
| 旧日志行（无 token 字段）反序列化为 null，天然兼容 | `Option<u64>` 缺省 + `usage_of` `:231-244` | 01 #7（旧行兼容测试 `parse_entries_defaults_missing_token_fields_to_none`、`aggregate_recent_requests_no_usage_rows_keep_null_token_fields`） | 达成 |
| Testing Decisions：aggregate 7 类用例 | 9 个新测试，见维度一 issue 01 #7 | 01 #7 | 达成 |
| Testing Decisions：WebUI 4 类用例 | `StatsPanel.test.tsx:272-433` | 02 #7 | 达成 |

### Out of Scope 检查 — **无越界**

| Out of Scope 项 | 结论 | 证据 |
|-----------------|------|------|
| TUI 请求明细（TUI 保持全量展示） | 未做，符合 | `4b90c1d` 仅改 `src-rust/stats.rs`；TUI 统计页代码未动 |
| 分页 / 滚动加载 / 排序选项 | 未做，符合 | 表格固定 `truncate(100)`、按响应顺序 |
| 对话详情视图 | 未做，符合 | 无相关代码 |
| 独立 `/requests` endpoint | 未做，符合 | `web.rs:72-110` 路由无新增，明细随 `/stats` 响应 |
| 导出变化 | 未做，符合 | `4b90c1d` 未触及导出逻辑；`csv_export_*` 为既有测试 |
| 请求明细按 provider/model 筛选 | 未做，符合 | 无筛选逻辑 |

### spec 内部张力（记录不追究）

- spec「Implementation Decisions」称 token 列/时间列「复用现有格式化函数」，但现有 `formatTokenDimension` 对 0 返回 `-`，与 User Story 11（真实 0 显示 0.0%/0）冲突；实现新增 `formatRequestToken` / `formatRequestTime`（`format.ts:39-55`）区分 null 与 0、缺失与非法时间。实现选择了 User Stories 的语义，行为更精确，issue 02 实施总结已记录该 Review 修复。结论：达成，实现方式较 spec 措辞更细。

**维度二结论：达成，无遗漏、无超范围。**

---

## 维度三：ADR 遵守 — **达成**

| ADR | 内容 | 核对 | 结论 |
|-----|------|------|------|
| ADR-0001（Token 通过 tee 流式解析获取，不缓冲响应） | 本 feature 未触碰采集路径：`usage_of` 仅读取日志字段（`stats.rs:231-244`），不改 tee/日志写入 | README.md:134 / README_ZH.md:133 描述一致 | 达成 |
| ADR-0002（对话边界由 `x-conversation-id` 头优先、body `conversation_id` 兜底） | 对话分组逻辑未改：`stats.rs:360-363` 仍按 `conversation_id` 分组、无标识归 `unlabeled`；README.md:142 / README_ZH.md:141 引用 ADR-0002 | 本 feature 未引入任何新的对话界定逻辑 | 达成 |
| ADR-0003（推理 token 是输出子集，total = input + output） | `stats.rs:441` 合计不含推理；cacheRate 公式 `cached ÷ input` 不含输出/推理 | README 缓存命中率条目（:141/:140）与 134 行推理子集表述一致 | 达成 |
| spec 声明「不新增 ADR」 | git log 无新 ADR 提交，`docs/adr/` 仍为 3 份 | — | 达成 |

附注（范围外，不影响本 feature 结论）：工作区存在未提交的 `src-rust/proxy.rs` 改动（`conversation_id_of` 增加 `x-opencode-session` 头兜底），落地后将扩展 ADR-0002 描述的对话标识来源，届时需同步 ADR-0002 与 README 表述。该改动不属于本 feature。

**维度三结论：达成，与 ADR-0001/0002/0003 全部一致。**

---

## 维度四：文档一致性 — **达成**

| 文档 | 核对 | 结论 |
|------|------|------|
| README.md:140（Request details 条目） | 与实现一致：窗口内每条请求、倒序、最近 100 条、10 项内容、失败带错误、无上报行显示 `-` | 达成 |
| README_ZH.md:139（请求明细条目） | 与 EN 对称，内容一致 | 达成 |
| README.md:139 / README_ZH.md:138（WebUI 面板条目） | 对话行六项两行布局已更新，与 `StatsPanel.tsx:191-207` 一致 | 达成 |
| README 缓存命中率条目（:141/:140） | 公式 `cached ÷ input` 与 `cache_rate_of` 一致；「无缓存数据显示 `-`」描述的是全局口径，行级 `0.0%` 语义在 140 行「无使用量上报的行显示 `-`，绝不冒充 0 测量」中不冲突 | 达成 |
| EN/ZH 双语对称 | 新条目两版一一对应（:139-140 / :138-139） | 达成 |
| spec.md Status（仍为 `ready-for-agent`） | 与上一 feature（`.scratch/token-usage-stats/spec.md` 同样保持 `ready-for-agent`）惯例一致；issue 04 checkbox 核对范围仅限 issue，不含 spec | 达成（按仓库惯例） |

**维度四结论：达成，README EN/ZH 已同步且与实现状态一致。**

---

## 问题清单

| 级别 | 问题 | 说明 |
|------|------|------|
| P0 / P1 | 无 | — |
| P2 | issue 03 checkbox 5「审计复核通过」勾选超前 | 标记 resolved 时审计尚未执行，实际由 issue 04 承接（`ready-for-agent`）。流程上有意拆分，属预期在途项，待 04 完成后闭环 |
| P3 | 工作区存在范围外未提交改动 | `src-rust/proxy.rs`（`x-opencode-session` 对话标识兜底）+ `webui/package-lock.json` 变更、`webui/dist/.gitkeep` 删除。与本 feature 无关，但 proxy.rs 改动落地后需同步 ADR-0002 与 README |
| P3 | spec 措辞「复用现有格式化函数」与实现「新增 `formatRequestToken`/`formatRequestTime`」差异 | 实现按 User Stories 语义区分 null 与 0，行为更精确，已在 issue 02 实施总结记录 |

## 总评

- 四维均达成。issue 01/02 全部 checkbox 经代码直读与独立测试复跑核实属实；issue 03 除「审计复核」一项部分达成（由 issue 04 承接，属预期）外全部达成；issue 04 未执行与 `ready-for-agent` 状态一致，属预期。
- spec 声明的交付内容全部实现且全部被 issue 覆盖，无遗漏、无超范围；Out of Scope 各项均未越界。
- 实现与 ADR-0001/0002/0003 完全一致；README EN/ZH 已同步。
- 唯一实质遗留：issue 04 尚未执行（本报告即其产出，后续需人工复核本报告并将 04 标记 resolved）。
