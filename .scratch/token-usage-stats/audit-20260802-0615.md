# 审计报告：feature `token-usage-stats`

- 审计时间：2026-08-02 06:15（+0800）
- 审计范围：`.scratch/token-usage-stats/`（spec.md + issues/01–07）
- 审计基线：`HEAD = 824d65e`（feat: show token usage stats in web UI dashboard，2026-08-02 06:11:20 +0800）
- 上次审计：`audit-20260802-0510.md`（HEAD = fad03b2，P2-1：05 未提交）

## 总体结论

**7 个 issue 全部实现并已提交，feature 主体交付完成。** 上次审计遗留的唯一 P2（05 WebUI 展示未提交）已解决——本次审计期间 06:11:20 提交 `824d65e`，WebUI token 展示与 16 个 vitest 用例已入库。Rust 侧 `cargo test --lib` 82/82 通过（L1 实测）。

无 P0/P1。遗留 2 个 P2（WebUI 测试从未实际运行、07 核对记录与 README 未跟进 05 已提交的事实）与 5 个 P3 观察项，均不阻断交付。

## 证据分级说明

- **L1（实测执行）**：`cargo test --lib` 82 passed / 0 failed；git 命令取证（log/show/status/ls-files）
- **L2（源码审读）**：824d65e 全部 11 个文件改动；stats.rs/usage.rs/proxy.rs/web.rs 关键路径；webui 前端实现
- **L3（文档比对）**：spec.md、issues/01–07、README.md、README_ZH.md、CONTEXT.md、docs/adr/0001、0002

## 一、完成度（逐 issue）

| Issue | 提交 | 状态 | 证据 |
|---|---|---|---|
| 01 pre-factor aggregate | 2e8203a | ✓ 完成 | `aggregate` 纯函数（stats.rs:210），`get_stats` 只读文件（stats.rs:350）；空/单/多/坏行测试 |
| 02 usage parser module | aafd243 | ✓ 完成 | usage.rs `extract_usage`（Anthropic > OpenAI > DeepSeek 探测）+ `SseUsageParser`；14 个测试 |
| 03 proxy capture | d5518b8 | ✓ 完成 | `StreamTee` 直通旁路 + `conversation_id_of` 头优先 body 兜底 + `build_log_entry` |
| 04 aggregation expansion | d580275 | ✓ 完成 | `UsageStats` 扩展 `totalTokens`/`cacheHitRate`/`byConversation` + CSV/JSON 导出 |
| 05 webui display | **824d65e** | ✓ 实现已提交（**测试未运行**，见 P2-1） | StatsPanel.tsx + types.ts + format.ts + 5+11 个 vitest 用例 |
| 06 tui display | 0b9de83 | ✓ 完成 | `render_stats` Tokens / Cache hit rate 行 + `token_summary` 7 个测试 |
| 07 wrap-up verify docs | fad03b2 | △ 文档已更新，**核对记录已过期**（见 P2-2） | README/README_ZH 更新；07 记录「05 未满足」与现状不符 |

**05 验收标准逐条核对（静态审读，因测试未运行，验证强度为 L2）：**
1. 四元素（Tokens 卡片 / Cache 率卡片 / by-provider Tokens 列 / By conversation 卡片）三状态渲染 —— 实现齐全，测试用例覆盖空数据、有数据、旧数据（dash）、旧后端缺字段、不回归五类（StatsPanel.test.tsx 5 个用例）
2. 无缓存数据显示 `-` —— `cacheHitRate ?? "-"` + `formatTotalTokens(0) → "-"`（format.test.ts 覆盖 undefined/0 两路径）
3. 前端类型与 Rust 序列化一致 —— types.ts `TokenTotals`/`ProviderStats`/`ConversationStats`/`UsageStats` 与 stats.rs serde camelCase 字段逐一对应（`json["totalTokens"]`、`json["cacheHitRate"]`、`json["byConversation"]` 测试佐证）
4. 现有统计功能不回归 —— Refresh / Export JSON / Export CSV 保留，`ui.tsx` Card 仅加可选 `style` prop（向后兼容）

## 二、Spec 遵守

- 统计口径：仅成功且非 retry、有 usage 的行计入 token（`usage_of`，stats.rs:194）；失败/重试/无 usage 行不计 —— 与 spec 一致，测试 `aggregate_sums_tokens_only_for_successful_non_retry_entries_with_usage` 覆盖
- 缓存命中率分母只含输入 token：`cached ÷ input`，无数据 `-` —— 一致（`aggregate_cache_hit_rate_is_cached_over_total_input`、`aggregate_cache_rate_is_dash_without_any_token_data`）
- 对话聚合：`x-conversation-id` 头优先 / body `conversation_id` 兜底 / 无标识归 `unlabeled` / 按最近活跃排序截断 top 20 —— 一致（`aggregate_conversations_*` 系列测试）
- 旧行兼容：`RequestLogEntry` 新字段 `#[serde(default)]`，CSV 缺失 token 留空 —— 一致（`csv_export_escapes_*`、`parse_entries_defaults_missing_token_fields_to_none`）
- WebUI 展示形态：Token 累计可读格式化（K/M/B/T，一位小数）、Cache 率卡片、provider Tokens 列、conversation 短 id 列表 —— 与 spec 一致（formatTokenCount 11 用例边界全覆盖，含 999_950 → "1.0M" 进位）
- API 路由不变（`/api/stats` 向后兼容追加字段）—— 一致

## 三、ADR 遵守

- **ADR-0001（tee 流式解析，不缓冲）**：`StreamTee` 复制响应流一份直通一份旁路解析，流结束补写日志 —— 遵守。测试 `stream_tee_forwards_chunks_unchanged_and_reports_usage`、`stream_tee_drop_mid_stream_still_reports` 佐证
- **ADR-0002（对话边界由客户端标识决定）**：头优先、body 兜底、无标识降级 `unlabeled`，不做启发式分组 —— 遵守。测试 `conversation_id_prefers_header_over_body` 等佐证
- 实现未偏离任一 ADR；ADR 文件无过期内容

## 四、文档一致性

- **CONTEXT.md**：术语（Token 使用量 / 缓存命中率 / 对话 / 未标记 / 请求日志）与实现一致，代码与文档均使用规范词（`cacheHitRate`、`unlabeled` 等）
- **README.md / README_ZH.md**：Usage Statistics 章节描述与实现一致，无过期信息；但**未提及 WebUI 的 token 展示**（07 更新文档时 05 未实现、刻意不写；现在 05 已提交仍未补）→ P2-2
- **issue 07 核对记录**：「05 WebUI 展示 — 未满足（显式记录）」一节已过期（824d65e 已提交实现）→ P2-2
- **07 记录数量偏差**：记 usage.rs「12 个测试」，实际 14 个（aafd243）→ P3-2
- **仓库文档版本控制**：`docs/adr/`、`CONTEXT.md`、`AGENTS.md`、`.scratch/` 均未跟踪（git status `??`），ADR 无版本历史可追溯 → P3-5

## 问题清单

### P2（需处理，不阻断交付）

- **P2-1 [WebUI 测试从未运行]**：824d65e 提交了 5 个 StatsPanel 用例 + 11 个 format 用例 + vitest 配置（vite.config.ts、package.json devDeps、test-setup.ts），但测试在任何环境都未执行过：本地无 `webui/node_modules`（无法 `npm test`），CI（`.github/workflows/ci.yml` verify job）只跑 `cargo test --release --lib` + `npm run build:webui`，**没有 vitest 步骤**。05 验收标准「三状态渲染正确」仅有静态审读证据（L2），无执行证据。静态审读逻辑正确（格式函数边界、断言值与 mock 数据自洽），预计 `npm install && npm test` 可绿；建议手动运行一次并在 CI 增加 vitest 步骤。
- **P2-2 [07 记录与 README 未跟进 05 已提交]**：07 的「05 未满足」记录已过期，应更新为「已满足（824d65e）」；README/README_ZH 的 Usage Statistics 章节应补充 WebUI 统计面板的 token 展示描述，保持用户侧文档与实现一致（这正是 07 验收标准 2 的未尽部分）。

### P3（观察项）

- **P3-1 [缓存率 NaN 边界]**：stats.rs:331-335，`total_cached > 0` 且 `total_input == 0`（异常 usage 数据，如 cached 有值而 prompt 为 0）时输出 `"NaN%"` 而非 `"-"` 或 `"0%"`。同上次审计，未修复。
- **P3-2 [07 测试计数偏差]**：07 记 02 为「12 个测试」，实际 14 个。
- **P3-3 [范围外行为措辞]**：OpenAI 非流式请求（`stream=false`）的 usage 不记录（tee 仅流式路径），README「Every proxied request … are parsed」措辞略宽于实现。
- **P3-4 [前端 NaN 边界]**：`StatsPanel.tsx:82` `formatProviderTokens(ps.promptTokens + ps.outputTokens)`，若后端是升级前版本（ProviderStats 无 token 字段），`undefined + undefined = NaN` → 显示 `"NaN"`。测试「tolerates an old backend that omits the token fields」的 mock 数据实际给了 `promptTokens: 0` 值而非缺失字段，**未覆盖真实缺失路径**（测试意图与数据不符）。当前 WebUI 与后端同包发布，触发概率低。上次审计的 P3（旧数据行显示 0）已在提交版本修复（`formatProviderTokens` 对 0 返回 `-`）。
- **P3-5 [仓库文档版本控制 + package-lock 卫生]**：824d65e 从 `.gitignore` 删除 `package-lock.json` 规则以提交 `webui/package-lock.json`，但根目录 `package-lock.json` 仍未跟踪（AGENTS.md 要求提交 package-lock.json）；`docs/adr/`、CONTEXT.md 等 feature 文档整体未跟踪，ADR 无 git 历史。

## 出口条件核对

- 四维审计（完成度 / spec 遵守 / ADR 遵守 / 文档一致性）：全部完成
- L1 证据：`cargo test --lib` 82/82 通过 ✓
- 未满足项：P2-1、P2-2 已列出并有后续动作建议；无遗漏的未满足验收项
- 报告已写入 `.scratch/token-usage-stats/audit-20260802-0615.md`

## 建议后续动作

1. `cd webui && npm install && npm test` 运行 vitest，确认 16 个用例通过；在 CI 增加 `npm test`（或 `npm run test`）步骤，让 WebUI 测试纳入自动验证
2. 更新 issue 07：将 05 标记为已满足（824d65e），补 WebUI 统计面板的验证记录；README/README_ZH 补充 WebUI token 展示说明
3. 视情况处理 P3：NaN 双端边界（Rust `-`、前端字段缺失回退）、07 测试计数、根 package-lock.json 提交
