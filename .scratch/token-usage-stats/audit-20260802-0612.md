# Issue 审计报告：审计issue 03（feature `token-usage-stats`）

审计时间：2026-08-02 06:12（精确到分钟；本环境 bash 权限禁止 `date`，以 HEAD 提交 824d65e 时间 06:11:20 为最近锚点推算）｜基线：audit-20260802-0510.md

> 目录说明：任务指令指定 `.scratch/审计issue 03 /`，该目录不存在；仓库中唯一的 feature 目录为 `.scratch/token-usage-stats/`（spec、issues/01–07、历史审计报告均在此），故本报告写入该目录，与历史基线并存。

## 结论总览
- 阻断项：0 项 / 非阻断项：7 项 → 通过
- 与上次审计对比：新增解决 2 项（P2-1 05 未提交 → 已由提交 824d65e 解决；P3-4 byProvider 旧数据 Tok 列显示 0 → 已修复为 `-`）/ 新引入 4 项（05 提交后产生的文档与验证观察项）/ 持续未满足 3 项（P3-1 NaN% 边界、P3-2 README 措辞、P3-3 12 vs 14 测试记录偏差）

## 维度 1 · 完成度（逐票）

### 01 prefactor-aggregate — 满足（commit `2e8203a`）
- [x] 聚合逻辑可独立调用：`aggregate(entries, circuit, cooldown_ms, now_ms)` 纯函数（src-rust/stats.rs:210），全部输入注入、无 I/O；`get_stats()` 只读文件后调用（stats.rs:350-362）
- [x] 重构前后统计结果一致：现有用例总量/成功率/平均延迟/by-provider/by-model/熔断状态全保留（`aggregate_single_success_entry_counts_everywhere`、`aggregate_computes_circuit_breaker_states_from_injected_time` 等）
- [x] 聚合函数带单测（空输入/单行/多行/异常行跳过）：`aggregate_empty_entries_yields_zero_stats`、`aggregate_single_success_entry_counts_everywhere`、`aggregate_multiple_entries_accumulates_groups`、`parse_entries_skips_empty_and_malformed_lines`
- [x] `cargo test` 全绿（本审计实测 82/82，L2）

### 02 usage-parser-module — 满足（commit `aafd243`）
- [x] 非流式提取三风格探测、顺序正确、只认第一个：`extract_usage_reads_all_three_field_styles`、`extract_usage_probes_cache_fields_in_order_and_takes_first`（usage.rs:33-41 顺序 Anthropic > OpenAI 标准 > DeepSeek）
- [x] 流式解析 OpenAI/Anthropic、任意 chunk 切分：`sse_parser_extracts_openai_usage_before_done`、`sse_parser_extracts_anthropic_usage_from_start_and_delta`、`sse_parser_matches_whole_stream_result_across_arbitrary_chunk_boundaries`、`sse_parser_handles_crlf_frame_separators`
- [x] 无 usage → 返回空不 panic：`sse_parser_returns_none_for_*`（3 个）、`sse_parser_tolerates_garbage_and_empty_input`、`extract_usage_handles_missing_or_malformed_usage`
- [x] 纯函数无副作用：usage.rs 仅内存 buffer + serde_json，无文件/网络 I/O（L1 代码审读）
- [x] 缓存率分母口径：`aggregate_cache_hit_rate_is_cached_over_total_input`（cached ÷ total input，输出不参与）
- 实际测试 14 个（非流式 3 + SSE 11）；issue 07 记录"12 个"不符（见非阻断项 3）

### 03 proxy-capture — 满足（commit `d5518b8`）
- [x] 流式转发体验不变（逐 token、无缓冲）：`stream_response`（proxy.rs:1100）经 `StreamTee`（proxy.rs:1024-1041）`poll_next` 原样转发 bytes 同时喂 parser；`stream_tee_forwards_chunks_unchanged_and_reports_usage`
- [x] 流结束日志含 token 与对话标识：tee 回调 `build_log_entry`（proxy.rs:1110-1113、1538）补写日志；`log_entry_includes_usage_and_conversation_when_present`
- [x] 转换路径日志含 token：OpenAI→Anthropic 非流式路径 `extract_usage` 后随日志行写出（proxy.rs:1235-1250）
- [x] 中断/无 usage 仍写行、token 字段留空：`stream_tee_drop_mid_stream_still_reports`、`stream_tee_drop_mid_stream_without_usage_reports_none`、`stream_tee_propagates_error_and_still_reports`、`stream_tee_reports_none_when_stream_has_no_usage`；`log_entry_leaves_token_fields_null_without_usage`
- [x] 旧行与新行共存无异常：`log_entry_roundtrips_through_request_log_entry`、`parse_entries_defaults_missing_token_fields_to_none`（RequestLogEntry 新字段 `#[serde(default)]`，stats.rs:19-23）
- 对话标识提取：`conversation_id_of`（proxy.rs:959-972）头优先 body 兜底、空值忽略；`conversation_id_prefers_header_over_body`、`conversation_id_falls_back_to_body_when_header_absent_or_empty`、`conversation_id_returns_none_when_unavailable_or_malformed`

### 04 aggregation-expansion — 满足（commit `d580275`）
- [x] 接口返回 `totalTokens`/`cacheHitRate`/`byConversation`/by-provider token 列、旧字段不变：UsageStats 结构 + `aggregate`（stats.rs:223-345）；`aggregate_provider_stats_accumulate_token_columns`
- [x] 聚合单测全绿：累计（`aggregate_sums_tokens_only_for_successful_non_retry_entries_with_usage`）、缓存率口径（`aggregate_cache_hit_rate_is_cached_over_total_input`）、Top 20 截断（`aggregate_conversations_sort_by_last_active_desc_and_truncate_top_20`）、unlabeled 合并（`aggregate_conversations_group_and_merge_unlabeled`）、失败行不计、旧行兼容
- [x] 无 token 数据：总量 0、缓存率 `-`、无空数组/空对象异常（`aggregate_no_token_data_serializes_empty_by_conversation`、`aggregate_cache_rate_is_dash_without_any_token_data`、`aggregate_cache_rate_is_dash_without_cache_data`）
- [x] CSV/JSON 导出：`csv_export_includes_token_and_conversation_columns`、`csv_export_escapes_conversation_id_and_omits_missing_tokens`、`json_export_serializes_token_and_conversation_fields`
- 计入口径：`usage_of`（stats.rs:194-206）仅 `ok==Some(true)` 且非 retry 且解析到 usage 的行

### 05 webui-display — 满足（commit `824d65e`，本次审计的关键变化：上次 P2-1 已解决）
- [x] 四个展示元素三状态渲染：StatsPanel.tsx 空状态（:37-42）、有数据（Tokens 卡片 :50、Cache 率卡片 :51、by-provider Tokens 列 :69/:82、By conversation 卡片 :92-112）、旧数据 `-` 展示；StatsPanel.test.tsx 5 个用例覆盖三状态 + 旧后端缺字段容错 + 现有指标/导出不回归
- [x] 缓存率无数据显示 `-` 不显示 `0%`：StatsPanel.tsx:51 `stats.cacheHitRate ?? "-"`；测试 `renders dashes for token metrics when only legacy data exists`
- [x] 前端类型一致：types.ts `TokenTotals`/`ProviderStats`/`ConversationStats`/`UsageStats`（:128-167）与 Rust 序列化字段（totalTokens/cacheHitRate/byConversation/promptTokens/outputTokens/cachedTokens/conversationId）一致；api.ts:42 `stats() → UsageStats`；App.tsx:114 stats tab 挂载
- [x] 现有功能不回归：测试用例 5 覆盖 Total/OK/Success/Export JSON/CSV/Refresh；ui.tsx `Card` 仅加可选 `style` prop（无破坏性改动）
- 可读格式化：`formatTokenCount`（K/M/B/T，format.ts:5-23）+ `formatTotalTokens`（total=0 → `-`）+ `shortConversationId`（12 字符截断），format.test.ts 12 个用例
- 证据说明：以上为 L1 静态核对；**vitest 测试实际执行结果未验证**（本环境 bash 权限禁止 npm/vitest；依赖已安装 `webui/node_modules/.bin/vitest` 存在、package.json `"test": "vitest run"` 已配置）→ 见待主 agent 验证清单

### 06 tui-display — 满足（commit `0b9de83`）
- [x] 统计页显示累计 input/output token 与缓存命中率：`render_stats` Overview 块追加 Tokens 行（pages.rs:383-391，`token_summary` :652-661）与 Cache hit rate 行（:392-396）
- [x] 无 token 数据 `-`、不崩溃不错位：`token_summary` total==0 → `-`；`token_summary_without_data_is_dash`、`token_summary_zero_input_is_still_rendered`
- [x] 与 WebUI 数字同源：均来自 `get_stats()`（stats.rs:350）；`token_summary_accepts_localized_words`

### 07 wrap-up-verify-and-docs — 满足（commit `fad03b2`）
- [x] 逐票核对 01–06：核对记录（2026-08-02）完整，05 当时"未满足"为真实状态且显式记录（无静默跳过）；该记录当前已过期（05 已由 824d65e 提交，见非阻断项 2）
- [x] 端到端链路验证：issue 07 记载隔离 HOME + mock SSE 上游实测（真实请求 → requests.log 含 promptTokens/completionTokens/cachedTokens/conversationId → `/api/stats` 数值手算一致 → TUI 数据源同源），WebUI 环节当时显式标记未满足。该记录与代码证据一致，采信为 L3 证据
- [x] README.md / README_ZH.md 同步更新：Usage Statistics/使用统计章节（README.md:132-140、README_ZH.md:131-139），缓存率口径、对话统计口径（含 unlabeled）、旧行兼容均与实现一致
- [x] 文档无过期信息：README 未声称 WebUI token 展示（未提 WebUI 面板，保守但不过期；见非阻断项 5）

## 维度 2 · Spec 遵守
- 满足。Implementation Decisions（spec.md:30-46）逐条核对：
  - usage.rs 纯函数模块与 `UsageSummary` 三字段 ✓（usage.rs:12-16）
  - 缓存字段探测顺序 Anthropic > OpenAI 标准 > DeepSeek ✓（usage.rs:33-41）
  - `SseUsageParser` 增量 push、OpenAI/Anthropic 两格式、无 usage 返回 None ✓（usage.rs:94-163）
  - `stream_response` tee 直通、流结束（含掐断）补写日志 ✓（proxy.rs:1100-1127 + StreamTee）
  - 转换路径非流式 `extract_usage` 随日志行写出 ✓（proxy.rs:1235-1250）
  - 对话标识头优先 body 兜底 ✓（proxy.rs:959-972）
  - `RequestLogEntry` 新字段 `#[serde(default)]` 旧行兼容 ✓（stats.rs:19-23）
  - 聚合重构纯函数、`get_stats` 只读文件 ✓（stats.rs:210/350）
  - `UsageStats` 扩展字段与序列化 camelCase ✓（totalTokens/cacheHitRate/byConversation/ProviderStats token 列）
  - 仅成功且 usage 行计入 ✓（`usage_of` stats.rs:194-206）
  - WebUI（spec:45）✓ 已提交 824d65e；TUI（spec:46）✓ pages.rs:383-396
- Testing Decisions（spec:52-57）：usage.rs 14 测试、aggregate 测试、缓存率口径测试全部真实存在且通过（L2）✓
- **Out of Scope 检查：未越过**（spec:62-66）：
  - JS legacy 不同步 ✓（src/proxy.js、src/stats.js 无 token 统计改动，grep 确认）
  - 对话级 UI 交互（删除/重命名/清空）✓ 未实现
  - 历史日志回填 ✓ 未实现
  - 费用估算、by-provider 缓存率 ✓ 未实现
  - 缓存率 per-request 明细 ✓ 未实现

## 维度 3 · ADR 遵守
- 0001 tee-parse-stream-for-token-usage：遵守。StreamTee `poll_next` 直通转发 + 旁路解析，无缓冲；流关闭/错误/Drop 后补写日志（proxy.rs:1024-1041、1100-1127；测试 `stream_tee_forwards_chunks_unchanged_and_reports_usage`、`stream_tee_drop_mid_stream_still_reports`）
- 0002 conversation-boundary-from-client-id：遵守。`conversation_id_of` 头优先、body 兜底、空值忽略；无标识归 `unlabeled` 合并组（proxy.rs:959-972；stats.rs:263-266；测试 `conversation_id_prefers_header_over_body`、`aggregate_conversations_group_and_merge_unlabeled`）

## 维度 4 · 文档一致性
- README.md：一致。Usage Statistics（:132-140）与实现吻合：requests.log 路径 `~/.pi-switch/`（与 `config_dir()` 一致，上次审计已核）、tee 不缓冲、缓存率口径（无缓存 `-` 非 `0%`）、对话标识来源（ADR-0002）、旧行兼容；目录结构含 usage.rs（:233）
- README_ZH.md：一致。使用统计章节（:131-139）与 README.md 及实现吻合
- WEBUI_GUIDE.md：一致。API 表（:116）`GET /api/stats` → `service::stats_value`，未声称 WebUI token 展示
- CONTEXT.md 术语：Token Usage / Cache Hit Rate / Conversation / Unlabeled / Request Log 与实现口径一致（分母只含输入、unlabeled 语义、x-conversation-id 头 + body conversation_id 兜底）
- 无过期信息、无声称未实现的功能；观察项：README 未提及 WebUI 统计面板（现在 05 已实现，属保守而非过期，见非阻断项 5）

## 未满足项清单（显式记录）
- [非阻断] 05 WebUI 测试（vitest）实际执行结果未验证：依赖已安装（webui/node_modules/.bin/vitest 存在）、`npm test` 脚本已配置，但本审计环境 bash 权限不允许执行 npm/vitest；测试正确性仅为 L1 静态审读。需要：主 agent 在 webui/ 下运行 `npm test` 与 `npm run typecheck` 确认全绿
- [非阻断] issue 07 核对记录"05 WebUI 展示 — 未满足"已过期：该记录是 fad03b2 时刻（07 完成时）的真实状态，824d65e 已提交 05 实现；issue 07 的 done 记录需修订（或按历史记录处理）。需要：更新 07 核对记录或由 triage 流程标记
- [非阻断] issue 07 记录 usage.rs"12 个测试"，实际 14 个（aafd243 提交说明亦写 12）——记录偏差，不影响实现
- [非阻断] 缓存率除零边界：`total_cached > 0` 而 `total_input == 0` 时输出 `"NaN%"`（stats.rs:331-335）。现实中 input ≥ cached 通常成立，概率极低；建议防御性回退 `-`
- [非阻断] README"every proxied request … token counts (when the upstream reports them) are parsed"措辞略宽于实际：OpenAI 兼容路径非流式（stream=false）请求 usage 不会被记录（tee 只解析 SSE 帧）。spec 仅要求转换路径非流式提取（spec:35），不构成违规
- [非阻断] README Usage Statistics 章节未提及 WebUI 统计面板：05 已实现，文档可补充 Stats 页的 Token/Cache 率/By conversation 展示描述（不补充不构成过期信息）
- [非阻断] 根 package-lock.json 仍未纳入 git 跟踪：824d65e 从 .gitignore 移除了 `package-lock.json` 忽略行（以提交 webui/package-lock.json），但根 package-lock.json 仍为未跟踪状态；与 AGENTS.md"提交 package-lock.json"约定不一致，与 feature 实现无直接关联

## 待主 agent 隔离验证（L3 条目）
- **WebUI vitest 测试套件**：升级条件 = subagent 环境权限不允许执行 npm/vitest（静态核对已满足，仅缺执行证据）。需验证：`cd webui && npm test`（5 个 StatsPanel 用例 + 12 个 format 用例全绿）、`npm run typecheck` 通过
- **WebUI 端到端渲染**（可选）：升级条件 = 需要 L3 佐证 05 验收标准 1 的"三状态渲染正确"。需验证：隔离 HOME 启动 webui + mock 上游（复用 issue 07 的 E2E 方法），Stats 页在有数据/无数据/旧数据三种日志状态下渲染正确。注：issue 07 的端到端实测到"统计接口 + TUI"为止，WebUI 面板渲染未实测过

## 备注
- 证据级别：完成度/Spec/ADR/文档一致性条目以 L1（静态读码 + git 提交记录）为主；L2 = `cargo test --lib` 本审计实测 82/82 通过、0 失败；L3 = issue 07 记录的隔离 E2E 实测（采信，与代码证据一致）+ 本报告待主 agent 验证清单
- 本次审计为只读审计：仅新建本报告文件 `.scratch/token-usage-stats/audit-20260802-0612.md`，未修改任何现有文档与代码
- 审计对象说明：任务指令目录名 `.scratch/审计issue 03 /` 与仓库实际 feature 目录 `.scratch/token-usage-stats/` 不一致，已按实际目录审计并写入
- 时间戳说明：环境无法执行 `date`，报告时间以 HEAD 提交 824d65e（2026-08-02 06:11:20 +0800）为最近锚点取整到 06:12
- 与上次审计（audit-20260802-0510.md，基线 HEAD=fad03b2）的差异根源：824d65e 于 06:11:20 提交 05 的 WebUI 实现（含 vitest 依赖、测试文件、package.json test 脚本），工作区现已干净

## 补充：主 agent L3 验证结果（2026-08-02 06:16）

WebUI vitest 已由主 agent 实际执行：`cd webui && npm test` → **2 测试文件 16 用例全部通过**（v4.1.10）；`npm run typecheck`（tsc --noEmit）零错误。

> 修正：报告正文预估"17 用例"，实际为 **16 用例**（StatsPanel 测试 5 + 转换/格式化测试 11，以实际运行为准）。
