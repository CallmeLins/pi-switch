# Issue 审计报告：cost-stats

审计时间：2026-08-04 01:00（精确到分钟）｜基线：无历史基线（`.scratch/cost-stats/` 下无 `audit-*.md`，本次为首份报告）

## 结论总览
- 阻断项：0 项 / 非阻断项：5 项 → **通过**
- 与上次审计对比：本次为首份报告

审计基线：HEAD `977d48e`；实现提交 `f07fe1f`（webui/stats/tui 主实现，00:09）、`bfeb6ec`（spec/issues 入库，00:26）、`2355d12`（proxy 请求时定格单价，00:35）；`b1f6748`/`977d48e` 属 conversation-id-inject feature（独立）。

## 维度 1 · 完成度（逐票）

### 01 proxy-cost-calculation（请求日志扩展 costTotal）— 满足
| 验收标准 | 判定 | 证据 |
|---|---|---|
| 成功且解析到 usage 的请求，日志行写入消费值，公式折算正确 | 满足 | `build_log_entry`（proxy.rs:1602-1624）：`cost_total = compute_cost(usage, cost)`；`compute_cost`（proxy.rs:1581-1597）：`uncached×input + cached×cacheRead + completion×output`，`uncached = prompt.saturating_sub(cached)`。测试 `log_entry_writes_cost_total_when_model_has_price`（proxy.rs:2113-2114，断言 costTotal 250.0）、`compute_cost_converts_cached_subset_at_cache_read_price`（cached 子集按 cacheRead 折算） |
| 分级档位按阈值生效 | 满足 | `compute_cost` proxy.rs:1584-1592：`filter(prompt >= t.input_tokens_above)` + `max_by` 取最高档，无档回落 base 单价。测试 `compute_cost_uses_tier_price_when_input_tokens_reach_threshold`（断言 85.0） |
| 模型未配置单价 → 消费 unknown，不写消费值 | 满足 | `lookup_model_cost`（proxy.rs:1567-1578）仅 model 有 cost 时返回 `Some`；`build_log_entry` match `(usage, cost)` 任一缺失 → `costTotal: null`。测试 `lookup_model_cost_returns_price_only_when_model_has_cost`、`log_entry_leaves_cost_total_null_without_price_or_usage`（断言 null） |
| 同口径：仅成功且非 retry 且解析到 usage 计入 | 满足 | 调用点 proxy.rs:1267/1365/1517 均在 `status.is_success()` 分支传 cost，失败/重试路径 cost 为 None；usage 缺失时 costTotal 恒 null（build_log_entry:1605-1608） |
| 旧行兼容（反序列化不崩） | 满足 | `RequestLogEntry.cost_total` `#[serde(rename = "costTotal", default)]`（stats.rs:29-30）；测试 `request_log_entry_deserializes_legacy_rows_without_cost_total`（stats.rs:776-786，断言 None） |
| 单测全绿 | 满足 | L2 实跑：`cargo test --lib` 129 passed / 0 failed（本审计执行） |

### 02 stats-aggregation-and-export（聚合与导出）— 满足
| 验收标准 | 判定 | 证据 |
|---|---|---|
| 统计接口新增总消费与 unknown 行数 | 满足 | `UsageStats.total_cost` / `cost_unknown`（stats.rs:135-138）；aggregate 在 `usage_of` 集合内求和（stats.rs:373-376）并计数 unknown。测试 `aggregate_sums_known_cost_and_counts_unknown_rows`（stats.rs:680-681） |
| 按对话聚合消费 | 满足 | `ConversationStats.cost: Option<f64>`（stats.rs:81-82）；对话级求和（stats.rs:407-409），全 unknown → None。测试 `aggregate_conversation_cost_sums_known_rows_and_stays_none_when_all_unknown` |
| 请求明细携带消费（unknown 空） | 满足 | `RecentRequest.cost: Option<f64>`（stats.rs:105-106）；`detail.cost = entry.cost_total`（stats.rs:460）。测试 `aggregate_recent_requests_carry_cost_or_none` |
| 同口径：总消费只含成功、非 retry、有 usage 行 | 满足 | 求和限定在 `usage_of`（stats.rs:241-254）返回集内，与 token 同口径 |
| 旧行兼容 | 满足 | 同 issue 01：serde default + `request_log_entry_deserializes_legacy_rows_without_cost_total` |
| CSV/JSON 导出追加消费列（旧行空） | 满足 | CSV 表头含 `costTotal`（stats.rs:569）、行值（stats.rs:591）；JSON 测试 `export_logs_json_serializes_cost_total`（stats.rs:804-812，断言 `json[0]["costTotal"] == 0.25`、legacy 行 null）。测试 `csv_of_includes_cost_total_column` |
| 单测全绿 | 满足 | L2 实跑：129 passed / 0 failed |

### 03 webui-cost-display（WebUI 消费展示）— 满足
| 验收标准 | 判定 | 证据 |
|---|---|---|
| 顶部消费卡片 + unknown 提示 | 满足 | `StatsPanel.tsx:191-198`（`Metric label="Cost"` + costUnknown 提示文案）；测试 `renders the total cost card with an unknown hint`（StatsPanel.test.tsx:145-146，totalCost 12.34 + costUnknown 2） |
| 对话列表消费列 | 满足 | `StatsPanel.tsx:257` `Cost {formatCost(c.cost)}`；测试 `shows the cost per conversation and per request`（StatsPanel.test.tsx:208-265，对话 cost 0.75 / 全 unknown 行 null） |
| 请求明细消费列 | 满足 | `StatsPanel.tsx:282` 表头、`:295` 行值 `["Cost", formatCost(r.cost)]` |
| 金额格式化规则（自适应精度） | 满足 | `webui/src/lib/format.ts` `formatCost`；`format.test.ts:132-155`：None→`-`、0→`$0.00`、0.0042→`$0.0042`、0.1→`$0.1`、1→`$1.00`、12.34→`$12.34`、1234→`$1.2K`、12_345_678→`$12.3M` |
| 全 unknown 总消费显示 `-` | 满足 | `formatCost(null)` → `-`；测试 `renders a dash for the cost card when the total cost is unknown`（StatsPanel.test.tsx:154-155） |
| 与 Tokens/Cache 卡片及窗口选择器并存 | 满足 | `StatsPanel.tsx` 窗口选择器（:120-138）→ token 卡片（:180-190）→ 消费卡片（:191-198）；既有测试 `keeps the existing request metrics and export actions` 仍在 |
| 前端测试全绿 | 满足 | 测试文件存在、断言逐条核对；主 agent 已实跑 `npx vitest run`（webui/）：**61 passed / 3 files / 0 failed**（见文末补充验证记录） |

### 04 webui-auto-refresh（WebUI 自动刷新）— 满足
| 验收标准 | 判定 | 证据 |
|---|---|---|
| 四档渲染、默认 Off | 满足 | `REFRESH_TIERS`（StatsPanel.tsx:17-22：Off/5s/30s/5min）；`refreshMs` 初始 null（:30）；测试 `offers the four auto-refresh tiers with Off selected by default`（StatsPanel.test.tsx:732） |
| 非 Off 按间隔刷新、沿用当前窗口 | 满足 | effect（StatsPanel.tsx:58-70）`setInterval` + `load(range, customFrom, customTo, true)` 沿用窗口参数（:63-66）；测试 `auto-refreshes on the selected interval reusing the current window`（:743） |
| 切回 Off 停止、切换档位重置计时 | 满足 | effect 依赖 `[refreshMs, range, customFrom, customTo, load]` 重建、清旧 interval；测试 `stops polling when switched back to Off`（:765） |
| 卸载清理 | 满足 | effect 返回 `clearInterval` 清理（:69）；测试 `clears the timer on unmount`（:797） |
| 刷新失败保留旧数据 | 满足 | `load(..., true)` keepOnError + catch 分支保留（:41-46）；测试 `keeps the current data when an auto-refresh fails`（:782） |
| 手动 Refresh 与 Off 档并存 | 满足 | 手动 Refresh 按钮（:141）在 Off 档下 `select(range)` 立即刷新，不受自动档位限制 |
| 前端测试全绿 | 满足 | 同 issue 03：主 agent 实跑 vitest 61 passed / 0 failed |

### 05 tui-total-cost（TUI 总消费）— 满足
| 验收标准 | 判定 | 证据 |
|---|---|---|
| Overview 新增总消费行 | 满足 | pages.rs:397-401（`stats_tokens` / `stats_cache_hit_rate` / `stats_cost` 三行并列渲染） |
| 全 unknown 显示 `-` | 满足 | `format_cost(None)` → `-`（pages.rs:657-659）；测试 `format_cost_renders_adaptive_precision` |
| 与 WebUI 同一格式化规则 | 满足 | pages.rs:653-672：0→`$0.00`、<$1 四小数去尾零、<$1000 两小数、≥$1000 走 `format_token_count` 缩写；测试 `format_cost_renders_adaptive_precision`、`format_cost_trims_sub_dollar_trailing_zeros` |
| i18n 中英文同步 | 满足 | i18n.rs:189 `pub fn stats_cost() -> &'static str { t!("Cost", "消费") }` |
| 单测全绿 | 满足 | L2 实跑：129 passed / 0 failed |

## 维度 2 · Spec 遵守
- **满足**。Implementation Decisions（spec.md:35-42）逐条落实：
  - 请求日志扩展 `costTotal` 可选字段、缺失 null → proxy.rs:1602-1624 ✓
  - 分级档位定价、`cacheWrite` 不参与（cacheWrite 无 token 数据，注释 proxy.rs:1579 明示，公式仅 input/output/cacheRead）✓
  - 请求完成时定格单价 → 请求开始时重载 profile（proxy.rs:1215 区域），单价随 `StreamLogFields.cost` 定格；提交 `2355d12` 即为该决策 ✓
  - WebUI 消费展示四件套（卡片/对话列/明细列/unknown 提示）→ StatsPanel.tsx:191-198/257/295 ✓
  - 自动刷新四档（Off 默认、沿用窗口、卸载清理、失败保留、手动并存）→ StatsPanel.tsx:17-70/141 ✓
  - TUI 总消费行 + 双端同规则 + i18n → pages.rs:397-401、i18n.rs:189 ✓
  - CSV/JSON 导出追加消费列 → stats.rs:569/591 + JSON 导出 ✓
  - webui types.ts 镜像同步 → types.ts:25（ModelCost）/160（ConversationStats.cost）/176（RecentRequest.cost）/189-190（totalCost/costUnknown）✓
- Testing Decisions（spec.md:44-51）：Rust 测试全部真实存在且本审计 L2 实跑通过（129 全绿，含 14 个 cost 相关测试）；WebUI 测试存在（StatsPanel.test.tsx cost 用例 4 个 + 刷新档位用例 4 个、format.test.ts formatCost 用例 9 个），断言静态核对一致。
- **Out of Scope 检查：未越过**。逐项核对：
  - 按 provider/model 拆分的消费列 → 未实现（by provider 表 StatsPanel.tsx:205-236 仍仅 token 列）✓
  - cacheWrite 单价计入消费 → 未实现 ✓
  - 对话消费端到端正确性（依赖 conversation-id-inject，另立 spec+tickets）→ 本 feature 仅交付聚合；注入插件已由 `b1f6748` 实现（该 feature 的 issue 02 未验收属另一 feature 状态，不影响本 feature 验收）✓
  - 货币换算 / 多币种 → 未实现 ✓
  - 消费分解明细（input/output/cached 分别计价）→ 未实现 ✓
  - 实时刷新过渡动画 → 未实现（静默更新）✓

## 维度 3 · ADR 遵守
- 0001 tee-parse-stream-for-token-usage：**遵守**。流式路径仍为 tee 直通、流结束补写日志（proxy.rs:1626-1628「Synchronous: callable from stream teardown paths where awaiting is not possible」）；cost 在日志构建时用定格单价计算，不触碰直通路径。
- 0002 conversation-boundary-from-client-id：**遵守**。三源：`x-conversation-id` → `x-opencode-session` → body `conversation_id`（proxy.rs:969-975）；聚合归组 key 缺失合并 `unlabeled`（stats.rs:381-384）；测试 `conversation_id_prefers_header_over_body`、`conversation_id_falls_back_to_opencode_session_header`、`conversation_id_falls_back_to_body_when_header_absent_or_empty`。README.md:142 / README_ZH.md:141 三源描述与实现一致。
- 0003 reasoning-tokens-subset-of-output：**遵守**。`total = input + output` 不重复累加推理（stats.rs:467 区域沿用既有口径）；消费计价使用 completion 全额（推理未单列计价，spec 亦未要求，口径无漂移）；cacheRate 公式不含输出。

## 维度 4 · 文档一致性
- README.md：无过期信息、无声称未实现的功能，但**新功能未写入文档**（非阻断）。统计章节（:126 功能表、:134-143）与 TUI 描述（:136「shows the cumulative input/output tokens and the cache hit rate」）、Stats API（:137，未提 `totalCost`/`costUnknown`）、WebUI 面板（:139，未提 Cost 卡片/对话消费列/自动刷新）、requests.log 条目（:250，未列 costTotal）均停留在 token 维度——所有既有描述仍真实，仅遗漏 cost-stats 新增内容。
- README_ZH.md：同上（:125/:131-142 未提消费与自动刷新）。
- WEBUI_GUIDE.md：架构性文档，未涉及统计页细节，无过期信息，一致。
- 无任何文档声称未实现的功能（无虚假声明）。

## 未满足项清单（显式记录）
- [非阻断] README（EN/ZH）未记录消费展示与自动刷新：统计章节（README.md:126,134-143 / README_ZH.md:125,131-142）未提 costTotal、Cost 卡片、unknown 提示、自动刷新四档。需要：按既有风格补充 feature 说明（发布前文档工作，不影响功能验收）。
- [非阻断] 亚美分坍缩 `$0`：`formatCost`/`format_cost` 对 <$0.005 的金额显示 `$0`（0.0042 → `$0.0042`，0.000042 → `$0`）；双端规则一致，review-standards.md 已记录。需要：人工判断是否接受。
- [非阻断] 跨千缩写边界：999.999 → `$1000.00` 不缩写（仅 ≥1000 才缩写）；`format_cost` 大额走 `format_token_count(cost as u64)` 整数截断。观感级，双端一致。
- ~~[非阻断] vitest 声称数字不一致~~ **已解决**：review-spec.md 称 54 passed vs issue 验收称 61 passed；主 agent 实跑 `npx vitest run`（webui/）确认为 **61 passed / 3 files / 0 failed**，61 为准，54 为陈旧自述。
- [非阻断] 工作树噪音变更未提交：`webui/package-lock.json`（-458 行，extraneous 平台包清理）、`webui/dist/.gitkeep` 删除仍在工作树（git status）。提交卫生问题，与 cost-stats 功能无关。

## 待主 agent 隔离验证（L3 条目）
- **issue 01 真实代理链路写 costTotal**：升级条件——验收标准明确「端到端行为：请求日志中带 token 使用量的行同时携带消费值」；L1 证据已强（build_log_entry 单测 + 三个调用点静态核对），升级为加固验证。需验证：临时 HOME + mock 上游（OpenAI 风格 chat.completions + usage 帧），经代理发流式与非流式请求各一次，检查 requests.log 行含 costTotal 且金额等于公式折算值（如单价 5/15/0.5 时 250.0）；模型未配单价的行 costTotal 为 null。
- **WebUI vitest 复跑**：~~升级条件——本会话 bash 权限仅允许 `cargo test --lib`，vitest 无法执行。~~ **已由主 agent 执行（2026-08-04 01:09）：`npx vitest run`（webui/）→ 3 files / 61 passed / 0 failed**，StatsPanel.test.tsx（cost 卡片 4 用例、刷新四档 4 用例）与 format.test.ts（formatCost 9 用例）全部通过，该条关闭。

## 补充验证记录（主 agent 执行，2026-08-04 01:09）
- 执行 `npx vitest run`（webui/）：**Test Files 3 passed / Tests 61 passed / Duration 1.06s / 0 failed**。
- 升级证据：issue 03「前端测试全绿」、issue 04「前端测试全绿」由 L1 静态核对升级为 L2 实跑；「vitest 声称数字不一致」非阻断项解决（61 为准）。
- 待验证清单第 2 条（WebUI vitest 复跑）关闭；仍待验证：issue 01 真实代理链路写 costTotal（L3，需临时 HOME + mock 上游，见上文）。

## 备注
- 证据级别：维度 1/2/3 主体为 L1（读代码 + git 提交历史 + 测试断言核对）；Rust 测试套件为 L2 实跑（`cargo test --lib`：**129 passed / 0 failed**）；WebUI vitest 初版报告为弱证据，后由主 agent 实跑升级为 L2（**61 passed / 0 failed**，见补充验证记录）；L3（真实代理链路）未执行，仍在待验证清单。
- 无历史审计基线，本次为首份报告。
- 澄清：review-spec.md 将 proxy.rs 新增 `x-opencode-session` 回退列为 scope creep——经 `git log -S` 查证该变更引入于 `b1f6748`（feat(extensions): inject session conversation id），属 conversation-id-inject feature 的合法提交，非 cost-stats 实现引入；不构成问题。
- 提交时序：`f07fe1f`（主实现）→ `bfeb6ec`（spec/issues 入库）→ `2355d12`（proxy 定格单价）→ `b1f6748`（conversation-id-inject）→ `977d48e`（HEAD，conversation-id-inject issue 01 resolved）。
- 本次审计为只读审计，仅新建本报告文件，未修改任何现有文档与代码。
